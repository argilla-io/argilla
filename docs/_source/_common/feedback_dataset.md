```{note}
The dataset class covered in this section is the `FeedbackDataset`. This fully configurable dataset will replace the `DatasetForTextClassification`, `DatasetForTokenClassification`, and `DatasetForText2Text` in Argilla 2.0. Not sure which dataset to use? Check out our section on [choosing a dataset](/practical_guides/choose_dataset).
```