name: pyre

on:
  push:
    branches: [ main, master, releases/* ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ main, master, releases/* ]


jobs:
  pyre:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2

      - name: Cache Conda env
        uses: actions/cache@v2
        with:
          path: ${{ env.CONDA }}/envs
          key: conda-${{ runner.os }}--${{ runner.arch }}--${{ steps.set-date.outputs.today }}--${{ env.CACHE_NUMBER }}
        env:
          # Increase this value to reset cache
          CACHE_NUMBER: 0
        id: cache

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install pyre-check~=0.9.0

      - name: Run Pyre
        continue-on-error: false
        run: |
          conda env list
          pyre --sequential --output=text check

#      - name: Expose SARIF Results
#        uses: actions/upload-artifact@v2
#        with:
#          name: SARIF Results
#          path: sarif.json
#
#      - name: Upload SARIF Results
#        uses: github/codeql-action/upload-sarif@v1
#        with:
#          sarif_file: sarif.json
#
#      - name: Fail Command On Errors
#        run: |
#          if [ "$(cat sarif.json | grep 'PYRE-ERROR')" != "" ]; then cat sarif.json && exit 1; fi